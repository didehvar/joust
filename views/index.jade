extends layout

block content
  h1= title

  if user
    | Welcome 
    a(href=user.profile_url) #{user.display_name}

    p
      img(src=user.avatar_full)

    a(href="/logout") Logout
  else
    include parts/steam
